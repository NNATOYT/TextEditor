export class Note {

    /**
     * 
     * @param {string} name The tittle of the note 
     * @param {string} text The text of the note
     */
    constructor(name,text) {
        this.name = name;
        this.text = text;
    }
}